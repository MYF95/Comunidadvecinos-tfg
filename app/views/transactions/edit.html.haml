- provide(:title, 'Actualizar movimiento bancario')
- provide(:button_text, 'Actualizar movimiento bancario')

%h2 Actualizar movimiento bancario #{@transaction.concept}
= form_for(@transaction) do |f|
  = render 'form', f: f